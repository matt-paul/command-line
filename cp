cp cp is used to copy a file from one directory to another.  It leaves the file that will be copied in its original place.  

It takes two arguments.

cp this_file this_file_copied

The file can be renamed as the copy is created.
