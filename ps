ps stands for process status and will show all processes that are occuring within that terminal.

ps

Adding an x (ie ps x) will show all processes currently occuring on the computer.

Status's can also be piped elsewhere, for example to a text file, or to further narrowing down, perhaps using grep.
